/*
========================================================================================
    Nextflow config file for running nf-test tests
========================================================================================
*/

params {
    modules_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/modules/data/'
    pipelines_testdata_base_path = 'https://raw.githubusercontent.com/nf-core/test-datasets/refs/heads/plasmodiumdrugres/'
}

aws.client.anonymous = true // fixes S3 access issues on self-hosted runners

// Prefer cached Wave images when already present locally.
docker.pullPolicy = 'ifNotPresent'

// Clear monolithic docker-profile containerOptions (e.g. forced linux/amd64) so
// per-module Wave images can run under the host/default platform selection.
process.containerOptions = null

conda {
    channels = ['conda-forge', 'bioconda']
}

// Always strip host pyenv shims so conda (and plain host) process CLIs are unambiguous.
process.beforeScript = '''
    export PATH="$(printf '%s' "$PATH" | tr ':' '\\n' | grep -v '/\\.pyenv/' | paste -sd ':' -)"
    unset PYENV_VERSION PYENV_VIRTUAL_ENV
'''
