nextflow_process {

    name "Test Process FEM_WRAPPER"
    script "modules/local/fem_wrapper.nf"
    process "FEM_WRAPPER"

    test("Should run without failures") {

        when {
            process {
                """
                input[0] = file("${projectDir}/tests/input/amino_acid_calls.tsv")
                input[1] = file("${projectDir}/tests/input/loci_groups.tsv")
                """
            }
        }

        then {
            assert process.success
            assert process.out.mlaf != null
            // unpack tuple output
            def (pop, outFile) = process.out.mlaf.get(0)

            assert pop == 'amino_acid_calls'

            // output md5 check
            assert path(outFile).md5 == "88d3a2c8207cbd704ac5038ca978ce99"
        }

    }

}
